/**
* @param {string} haystack
 * @param {string} needle
 * @return {number}
 */

//indexOf() returns -1 if no occurrence.
let strStr = function(haystack, needle) {
    return haystack.indexOf(needle);
};

// let str = 'Hello'
// let ned = 'He'

// console.log(strStr(str, ned));
